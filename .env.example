# Compiler tool paths
# Copy this file to .env and modify as needed for your system

# Path to LLVM's llc compiler (IR to assembly)
LLC_PATH=/opt/homebrew/opt/llvm@18/bin/llc

# Path to clang compiler (IR to executable)
CLANG_PATH=clang

# For Linux users, paths might be:
# LLC_PATH=/usr/bin/llc
# CLANG_PATH=/usr/bin/clang

# For Windows users with LLVM installed via Chocolatey:
# LLC_PATH=C:/ProgramData/chocolatey/lib/llvm/tools/llc.exe
# CLANG_PATH=C:/ProgramData/chocolatey/lib/llvm/tools/clang.exe 